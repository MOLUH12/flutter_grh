import 'package:flutter/material.dart';

class CongesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Gestion des Congés')),
      body: Center(child: Text('Gestion des Congés')),
    );
  }
}
